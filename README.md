# APE MVP documentation

Prebuilt static APE documentation. No npm install or build is needed to host these files.

GitHub Pages: select **Deploy from a branch**, branch **main**, folder **/ (root)**.

Expected address: https://danielius-tvarijonas-vinted.github.io/ape-mvp-docs/

The site uses `/ape-mvp-docs/` as its base path. Serve it at that path when previewing locally.

This snapshot includes only the APE documentation and its search index. Internal content planning is excluded.
